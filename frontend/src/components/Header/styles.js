import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    header: {
      borderRadius: 15,
      margin: '30px 0',
      display: 'flex',
      alignItems: 'center',
      padding: '10px 50px',
      background: '#000',
      color: 'white',
      border: '3px solid white'
    }
  }));